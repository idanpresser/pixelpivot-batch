# Canonical air-gap dependency list for PixelPivot Batch.
#
# Consumed by:
#   - scripts/download_wheels.ps1 (host-side: pre-fetch wheels into vendor/wheels)
#   - scripts/sandbox_init.ps1    (sandbox-side: install from vendor/wheels)
#
# Format: one package spec per line. Lines starting with # are comments.
# Edit here, not in either PowerShell script.

# --- Build-time tooling (must install first / be available during builds) ---
setuptools
wheel
pip
cffi
pkgconfig

# --- Application dependencies (mirror pyproject.toml) ---
fastapi
uvicorn[standard]
httpx
pydantic
watchdog
Pillow
pillow-heif
pyvips
pandas
psutil
nvidia-ml-py
prompt-toolkit
rich


# --- Dev / smoke-test deps (mirror pyproject.toml [project.optional-dependencies].dev) ---
# Included so the air-gapped target can run `pytest` and scripts/smoke_test.ps1.
# Drop this section if you are shipping a production-only bundle.
pytest
pytest-asyncio
respx
wand
