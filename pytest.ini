[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    smoke: end-to-end smoke tests that exercise real external binaries (skipped automatically when the binary is absent)
    integration: tests that integrate multiple subsystems, potentially requiring native libraries

